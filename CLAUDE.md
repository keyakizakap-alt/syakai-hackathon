## 方針の正本

このリポジトリの進め方と設計判断は、2 つのスキルに集約してある。
複製せず参照する（複製すると更新時に内容がずれるため）。

- **`work-directives`** — 作業の進め方、確認を取る操作、成果物の禁則、リサーチの裏取り
- **`ai-product-playbook`** — LLM の境界、API の防御、出荷と品質ゲート

正本: [keyakizakap-alt/dxworkrepository](https://github.com/keyakizakap-alt/dxworkrepository) の
`.claude/skills/`（全プロジェクトで自動ロードさせる手順は同リポジトリの `README.md`）。

**作業を始める前に `work-directives`、設計を変える前に `ai-product-playbook` を読むこと。**

## このリポジトリで壊してはいけないもの

- **フェイルオープン禁止（最重要）**：判定が取れなかった作品は遮断、判定が取れなかった
  文化圏は green にせず yellow へ倒して未検査である旨を明記する。4 文化すべて失敗したら
  結果を返さずエラーにする。「200 OK で全部 green」は最も避けるべき応答。
- **ユーザー入力を検閲・削除しない。** 入力は判定対象そのもので、削ると原文と結果が対応しなくなる。
  守るのは 3 層（山括弧の全角化 / 不可視文字の除去 / `UNTRUSTED_INPUT_RULE`）で行う。
- **U+200D（ZWJ）を落とさない。** 絵文字の連結に使われており、消すと 💀 や 👨‍👩‍👧 が化ける。
  絵文字の解釈が核心の製品なので致命的。
- **個人アカウントの分析・スコアリングをしない。削除も通報もしない。** 判定は発信者本人にだけ返す。
- 説教しない（`TONE_RULE`）。これを外すと上から目線の説教 bot になる。
- API キー無しでもデモモードで動く状態を保つ。`lib/api-guard.ts` を迂回する API ルートを作らない。
